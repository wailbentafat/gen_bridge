import 'package:flutter/material.dart';

class Comingev extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pastev'),
      ),
    );
  }
}